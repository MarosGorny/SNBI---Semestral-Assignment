classdef AudioProcessor
    methods(Static)
        function processedData = process(data)
            % Sem pridaj kód na spracovanie zvuku
            processedData = data; % Toto je len placeholder
        end
    end
end
